```c++
// 回溯算法研究的是树枝，树枝上的值就是所做的选择
void traverse(参数){

    if (条件满足){
        //
        return;
    }

    for ( ; ; ) {// for循环中就是树枝
        if (!isRightStr(s.substr(0,len))) continue;// 判断是否合法

        path.push_back(选择);// 做选择
        traverse(参数);
        path.pop_back();// 撤销选择
    }

}
```
