```c++
// 回溯算法研究的是树枝，树枝上的值就是所做的选择
void traverse(参数){

    if (条件满足){
        //
        return;
    }

    for ( ; ; ) {// for循环中就是树枝
        if (!isRightStr(s.substr(0,len))) continue;// 判断是否合法

        path.push_back(选择);// 做选择
        traverse(参数);
        path.pop_back();// 撤销选择
    }

}

// 子集/组合，使用beg控制选择开始的位置以去重
// 排列，使用used记录已经使用过的元素或已经使用过的元素的位置以去重
// 有重复元素的子集/组合，beg + 排序 + 剪掉重复树枝
// 有重复元素的排列，used记录已经使用过的元素的位置 + hasPick记录已经使用过的值
```
