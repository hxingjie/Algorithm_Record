```c++
int find(int idx){
    if (tree[idx] != -1){
        tree[idx] = find(tree,tree[idx]);
        return tree[idx];
    }else{
        return idx;
    }
}

void unionNode(int l,int r){
    int ll = find(tree,l);
    int rr = find(tree,r);
    if (ll != rr)
        tree[rr] = ll;
}
```

