```c#
// 差分数组
// nums 8  5  9  6  1
// diff 8 -3  4 -3 -5

// 构造差分数组
diff[0] = nums[0];// ！！
for (int i = 1; i < diff.Length; i++)
{
    diff[i] = nums[i] - nums[i-1];
}
// 构造差分数组

// 使得nums的[i, j]增加inc(inc+-都行)
diff[i] += inc;
diff[j+1] -= inc;// j+1 < diff.Length 时才执行此操作

// diff 8 -3  4 -3 -5
// nums 8  5  9  6  1
// 反推原始数组
nums[0] = diff[0];
for (int i = 1; i < nums.Length; i++)
{
    nums[i] = nums[i-1] + diff[i];
}
// 反推原始数组
```

